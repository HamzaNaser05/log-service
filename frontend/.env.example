# Browser-facing base URL. The default /api uses the local proxy.
VITE_API_BASE_URL=/api

# Vite development proxy target; not embedded in the production bundle.
VITE_DEV_API_TARGET=http://localhost:8080
